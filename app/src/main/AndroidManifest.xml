<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FH"
        tools:targetApi="31">
        <activity
            android:name=".notifications.NotificationsActivity"
            android:exported="false" />
        <activity
            android:name=".user.FollowListActivity"
            android:exported="false" />
        <activity
            android:name=".main.AccountSetupActivity"
            android:exported="false" />
        <activity
            android:name=".user.UserViewActivity"
            android:exported="false" />
        <activity
            android:name=".posts.comments.CommentsActivity"
            android:exported="false" />
        <activity
            android:name=".posts.AddNewPostActivity"
            android:exported="false" />
        <activity
            android:name=".posts.PostsActivity"
            android:exported="true" />
        <activity
            android:name=".main.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".main.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>